# -----------------------------
# .gitignore для .NET / ASP.NET Core
# macOS (включая M1) + VS Code
# -----------------------------

# === Build outputs ===
[Bb]in/
[Oo]bj/
artifacts/
publish/
*.Publish.xml

# === Visual Studio / Rider / IDE files ===
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates

# JetBrains Rider
.idea/
*.sln.iml
*.DotSettings.user

# ReSharper
_ReSharper*/
*.[Rr]e[Ss]harper

# === Visual Studio Code ===
.vscode/
*.code-workspace

# (если хотите отслеживать специфичные рабочие настройки, удалите из .gitignore нужные файлы в .vscode)

# === NuGet / packages ===
*.nupkg
project.lock.json
project.fragment.lock.json
.nuget/
packages/

# Paket
.paket/
paket-files/
paket.dependencies.lock

# === Dotnet user / local secrets & settings ===
# User secrets (ASP.NET Core user-secrets are stored outside proj but на всякий случай)
secrets.json
**/secrets.json

# Development settings (локальные конфиґи)
appsettings.Development.json
appsettings.Local*.json
appsettings.*.Local.json

# launch/settings generated by tooling
Properties/launchSettings.json

# === Logs & DB files ===
*.log
logs/
*.sqlite
*.db
*.sdf

# === OS files (macOS / Linux / Windows) ===
# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# Thumbs.db (Windows)
Thumbs.db
ehthumbs.db

# Temporary files
*.tmp
*.temp
*.bak
*.swp
*~.nib

# === Docker / Environment ===
# Ignore local environment files that may contain secrets
.env
.env.local
docker-compose.override.yml

# === Generated files / tooling ===
# publish output, generated code, analyzers caches
*.log
*.cache
*.class
*.exe
*.dll
*.pdb
*.so
*.dylib
*.bin

# dotnet tool manifest local cache
.tools/

# Rider / VS caches
.vscode-test/
.vscode-tasks/

# === Optional: if you use GitHub Actions artifacts ===
*.zip
*.tar.gz

# === Misc ===
# Avoid committing node_modules if frontend is present
node_modules/

# End of file
